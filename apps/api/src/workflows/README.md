# Workflows

This directory now contains application-level orchestration for cross-module flows.

Current synchronous workflows:

- `recommendation/build-home-recommendations.ts`
- `schedule/build-schedule-timeline.ts`

Planned future async workflows:

- daily recommendation refreshes
- content generation
- scheduled digests
