# Admin Placeholder

`apps/admin` 当前仅作为未来管理后台的预留目录。

暂不实现以下能力：

- 动态菜单平台
- 复杂角色权限后台
- 内容审核后台页面
- 数据监控后台页面

后续若需要实现管理端，建议保持：

- `apps/web`: 用户端
- `apps/admin`: 管理端
- `apps/api`: 统一后端

不要把管理端专属逻辑直接写回用户端前端工程。
